@model HackerCentral.ViewModels.UsersViewModel
@{
    ViewBag.Title = "Users";
}

<div class="container">
    <h2>Users</h2>

    @foreach (var u in Model.Users)
    {
        <div>@u.username (@u.id)</div>
    }
</div>
